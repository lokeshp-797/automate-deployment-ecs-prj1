# Local .terraform directory (provider binaries and modules)
jenkins-deployment/.DS_Store
ecs-deployment/.DS_Store
.DS_Store
*.pem
.terraform/
jenkins-deployment/.DS_Store
# State files (NEVER commit these; use a remote backend like S3)
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Variable files (Likely to contain sensitive data)
*.tfvars
*.tfvars.json

# Override files (Local-only changes)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# CLI configuration files
.terraformrc
terraform.rc